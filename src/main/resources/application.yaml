server:
  port: 9090

spring:
  application:
    name: book-my-ticket

  datasource:
    url: jdbc:mysql://localhost:3306/book-my-ticket?createDatabaseIfNotExist=true
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        '[format_sql]': true

  data:
    redis:
      url: redis://localhost:6379

  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB

  mail:
    host: smtp.gmail.com
    port: 587
    username: sainathagalave69@gmail.com
    password: vwcofgizwwscvkjv
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
        debug: true

admin:
  email: admin@gmail.com
  password: admin

cloudinary:
  url: ${CLOUDINARY_URL:}